# Design-doc release log

Tracks updates to `xiaoguai-agent-design/docs/` over time. Entries are
newest-first.

| Date | Change | Source PR(s) / sessions |
|---|---|---|
| 2026-05-30 | **Sprint-11 Step 1 — close 3 LLD-vs-impl UI drifts surfaced by sprint-10b e2e.** No new HLD DECs; the LLDs already specify the missing UI — implementation just hasn't caught up. Adds three "Sprint-11 status (2026-05-30) — drift to close" callouts that embed gap-close checklists (each tied to the matching `test.fixme()` in `frontend/e2e/tests/...`): (1) `lld-admin-ui.md` §4.2 — Audit pane lacks `<ChainBadge>` column + Export action (sprint-11 task S11-1); backend `POST /v1/audit/exports` already mounted (sprint-7 PR #74). (2) `lld-chat-ui.md` §4.3.1 (new sub-section) — HotL inline Approve / Deny / Approve-and-remember buttons not yet exposed; current `HotlBanner` is link-out only (sprint-11 task S11-3). (3) `lld-chat-ui.md` §4.7.1 (new sub-section) + §4.7 failure-mode row updated — SSE reconnect banner unimplemented; `XiaoguaiClient.sendMessage()` calls `onError` once and stops (sprint-11 task S11-2). All three callouts confirm the existing prose is correct and reaffirm the DEC-LLD relationships they refine; no decision rows added. `updated_at` bumped to 2026-05-30 on both LLDs. | Sprint-11 Step 1 PR pending |
| 2026-05-29 | **Sprint-10 Step 1 — SLO module LLD detail (DEC-022).** Per workflow rule, architecture detail lands before tasks. Adds new `lld/lld-observability.md` (`LLD-OBS-001`) covering the SLO module on top of the existing `xiaoguai-observability` Prometheus stack: §2 public interface (`Slo` / `Signal` / `ContractSurface` / `BurnRateCalculator`), §4.2 maps the four golden signals to actual registered metrics (`xiaoguai_http_request_duration_seconds`, `xiaoguai_hotl_usage_total{verdict}`, `xiaoguai_tokens_consumed_total` vs `tenant_daily_budget`, etc.) with a footnote on the DEC-022 prose mismatch on `request_duration_seconds`, §4.3 default SLO table verbatim from DEC-022, §4.4 `xiaoguai_slo_burn_rate{signal,window}` gauge cardinality + computation, §4.5 two-window 1 h+6 h burn-rate alert pattern (DEC-022 floor; wave-3's richer 4-pair scheme remains an opt-in), §4.6 wave-3 precursor relationship (no migration — parallel `slo-api-tier-*` groups added alongside `wave3-slo-*`), §4.7 per-tenant override via existing `tenant_settings.settings` JSONB (mirrors DEC-019 `sandbox_tier` lenient-parse pattern; no new migration), §4.8 alert routing through existing `xiaoguai-watch` + alertmanager pipeline (DEC-022 "no new alerting plumbing"), §4.9 failure modes, §7 unit + integration + system test cases. `harness-engineering.md` §16 gains an SLO-framing paragraph after the four-bucket table — the buckets stay, SLOs frame the Service quality bucket as contracts. `lld/index.md` gains the observability row. | xiaoguai-agent-design#5 |
| 2026-05-29 | **Sprint-10b Step 1 — frontend architecture documentation (DEC-025 + dual-SPA LLDs).** Retro-actively encodes the architecture of the `frontend/` monorepo that has been growing organically since v1.3. HLD §2 module map gains four frontend package rows (`@xiaoguai/admin-ui`, `@xiaoguai/chat-ui`, `@xiaoguai/shared`, `@xiaoguai/e2e`). HLD §3 adds **DEC-025** — dual-SPA split (admin vs chat) sharing one `XiaoguaiClient`; trust-boundary rationale; no in-SPA login (delegated to proxy / OIDC); state pane-local + URL; RBAC scope hints are UX-only (backend Casbin enforces). New `lld/lld-admin-ui.md` (LLD-ADMIN-UI-001) documents the 14 existing panes + 2 sprint-10b additions (Personas, Skill Proposals), pane lifecycle, Audit HMAC chain visualisation, Kanban / Scheduler / HotL Policies flows, `<RequireScope>` gate component, i18n + a11y, failure modes. New `lld/lld-chat-ui.md` (LLD-CHAT-UI-001) documents the 3 routes (ChatPage default / sessions/:id / skills), composer-doesn't-block-on-SSE, HotL pending inline panel, AI disclosure mandatory-by-default, partial-bubble-preservation on SSE drop, watch indicator. PRD gains §4.14 (REQ-UI-001..008) + §5 NFR-011..014 (first-token P95, multi-browser, bundle size, a11y). test-spec gains §3.14 with 18 UI golden-path cases. lld/index.md gains two frontend rows. Sprint-10b is **parallel** to sprint-10 (SLO) and shares the v1.8.0 release window. | Sprint-10b Step 1 PR pending |
| 2026-05-31 | **Sprint-9 Step 1 — multi-agent triangle LLD detail.** Per workflow rule, architecture detail goes in before tasks. Expanded `lld/lld-orchestrator.md` with new `Triangle` pattern: §1 adds the role table; §2 extends `OrchestrationPattern` enum + introduces `TriangleBudget`; §3 module structure adds `triangle/` submodule (plan/roles/verdict/scratchpad/memory_view/budget); §4.4 spells out the full planner→worker→critic→{Approve|RequestRevision|Reject} loop; §4.5 documents `MemoryView` (read-only round-frozen snapshot) + `Scratchpad` (per-task quarantine to prevent cross-Worker contamination); §4.6 budget split enforcement; §4.7 failure modes table. §7 test design lists 6 new triangle integration cases. `lld/lld-personas.md` adds v1.6+ note — the three roles are regular personas with a `role/` naming convention; migration `0025_persona_role_tags.sql` adds the optional `tags` column. `harness-engineering.md` §13 planning modes adds the triangle row with when-to-use rules. | Sprint-9 Step 1 PR pending |
| 2026-05-30 | **Sprint 8–10 architecture decisions added + DEC-024 MiniMax provider.** Following user's workflow rule "先更新架构文档，再安排任务" (sprint-workflow.md): HLD §3 gains **DEC-019** (`ExecBackend` trait extraction enabling per-tenant L1↔L3 swap; sprint-8 prereq), **DEC-020** (L3 implementation in single `xiaoguai-mcp-exec-wasm` crate hosting Python + JS modules sharing wasmtime engine), **DEC-021** (multi-agent topology = planner/worker/critic triangle with shared MemoryView + 50/40/10 budget split), **DEC-022** (4 golden signals + SLO contracts as first-class with burn-rate alerts), **DEC-023** (Tier-2/3 follow-up hardening: AES-GCM refresh tokens, typst PDF backend, T3 production wiring, T6 agent-loop integration), **DEC-024** (new MinimaxBackend — 9th provider — dedicated impl not OpenAiCompat wrapper, supports M1/M2/abab models + reasoning_content thinking-mode passthrough; lands as sprint-8 S8-10). HLD §2 module map updated: `xiaoguai-llm` row now says "9 provider backends". | Sprint-8 planning (PR #1 + PR #77) |
| 2026-05-29 (final) | **Sprint-7 design-doc sync — T3/T4/T5/T6 + JS sandbox + L3 decision.** Closes the gap identified by user 2026-05-29: 5 functional PRs (#72–#75 + ADR-0020) shipped but design docs hadn't been updated beyond compaction. Updates: **HLD §2** module map now 32 crates (new `xiaoguai-mcp-exec-js`), with annotations on `xiaoguai-audit` (export module T5), `xiaoguai-mcp` (OAuth PKCE T4), `xiaoguai-agent` (propose_skill T3), `xiaoguai-tasks` (skill_author T3), `xiaoguai-api` (new routes T3+T5), `xiaoguai-cli` (new subcommands T3+T4+T5). **HLD §3** new DEC-014 (agent-authored skills whitelist-only + HotL + admin), DEC-015 (OAuth PKCE hand-rolled + RLS tokens + refresh-rotation), DEC-016 (compliance export chain-verify-non-bypassable + ChainProof), DEC-017 (polyglot sandbox separate crates per language), DEC-018 (L3 = wasmtime+pyodide per ADR-0020). **LLDs**: `lld-mcp.md` new §4.4 OAuth PKCE module + new error variants. `lld-audit.md` new §4.4 compliance export module + `AuditChainExporter` trait. `lld-mcp-exec.md` updated §1 with sibling-crate note + L3 future reference. **New LLDs**: `lld-mcp-exec-js.md` (T6 full LLD), `lld-tasks.md` (skill_author sub-module + Kanban ADR-0019 reference). `lld-agent.md` new §4.2a propose_skill synthetic tool. **test-spec.md** §3.3 adds CASE-MCPEXEC-JS-001..004 + CASE-MCPAUTH-001..006; §3.7 adds CASE-AUD-EXP-001..008; new §3.8a adds CASE-SKILLAUTH-001..010. **test-strategy.md** §8 adds RISK-SEC-005 (skill author abuse), RISK-SEC-006 (OAuth refresh-token leak), RISK-COMP-001 (audit export over tampered window), RISK-SEC-007 (polyglot cross-runtime escape). **harness-engineering.md** §14 sandbox-tiering table now lists Python L1 + JS L1 separately and references DEC-017/-018; §16 metrics adds 4 new Prometheus series across the new features; §17 anti-patterns adds four new rejected practices (agent-author bypassable gates, compliance bypass, wide OAuth scope, cross-language trust sharing). | PR #72/#73/#74/#75/#76 + design-doc PR (this) |
| 2026-05-28 (later) | **Enriched `harness-engineering.md`** from the TRAE long-form writeup (理解 Harness Engineering, 2026-04-10). Six new sections: §4 PPAF cycle, §5 four-dimensional harness (造缰/驭马/相马/育马), §6 strategic 2D matrix, §7 three core constraints, §9 the three formal harness components (Context Manager / Call Interceptor / Feedback Assembler), §12 Function Calling lifecycle. Token-transformation pipeline (§11) gains explicit Reduction Rules + Injection Boundary terminology. Top-of-doc "at a glance" index added. Doc now 533 lines / 18 sections + References (was 336 / 12). | Same session, ad-hoc enrichment |
| 2026-05-28 | **Phase 1 prep of the post-session-5 update pass.** Added `harness-engineering.md` (R.E.S.T model, REPL-container abstraction, control/data plane split, sandbox tiering, six design principles). Cross-linked from `hld.md` top. Staged `HANDOFF-DESIGN-UPDATE.md` for Phase 2 in a fresh session. Implementation-repo PR `xiaoguai/docs/architecture/design-link.md` makes the design dir discoverable from the code repo. | Implementation PRs: xiaoguai-agent/xiaoguai#65 (plans) · #66 (Plan D.2 compaction) · #67 (Plan B systemd + README) · #68 (Plan A demo prep) · #69 (Plan C design-link, if assigned) |
| 2026-05-28 | _Phase 2 of the post-session-5 update pass — pending. Will land HLD §2 cache-fallback row, HLD §6.5 HotL/policy-gateway section, PRD sandboxed-code-execution sub-requirement, guardrails redact-before-HMAC rule, lld-agent HotL gate subsection, and the `adr/` directory mirroring the implementation ADRs. Driven from a fresh `claude` session per plan C._ | TBD |
| 2026-05-28 (earlier) | v1.4 retrofit pass — initial population of `hld.md`, `prd-xiaoguai.md`, `api-contract.md`, `guardrails.md`, `runbook.md`, `test-spec.md`, `test-strategy.md`, and `lld/*.md` (12 files). | Initial retrofit, not tracked under PRs |
